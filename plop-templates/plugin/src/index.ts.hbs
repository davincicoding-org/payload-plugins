import type { Plugin } from 'payload';
import type { {{pascalCase name}}PluginConfig } from './types';

export const {{camelCase name}}Plugin =
  (config: {{pascalCase name}}PluginConfig): Plugin =>
  (payloadConfig) => {
    return payloadConfig;
  };
